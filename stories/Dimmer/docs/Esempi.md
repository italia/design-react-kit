The Dimmer is made up of a general wrapper `<Dimmer>` and `<DimmerButtons>` for inner actions.

The text is contained within a tag `<p>` and can be preceded by an icon by passing `iconName` in `<Dimmer>`.