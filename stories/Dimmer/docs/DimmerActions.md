A Dimmer can contain keys linked to actions and a descriptive title.

The buttons are inserted in `<DimmerButtons>` and a prop `dark` is passed. If there is only one button, add the prop `single` to it.