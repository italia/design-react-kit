import { Meta } from '@storybook/blocks';

<Meta title='Documentazione/Changelog' />

# Changelog

In questa pagina è possibile trovare la lista dei cambiamenti da versione a versione del kit.

Alcuni dei cambiamenti sono accompagnati da alcuni simboli grafici.
Legenda:

- 💥 breaking change
- 🗑 deprecato

# Release v.4.0.0

### Cambiamenti importanti

- È stato incrementata la versione minima di React supportata come peer-dependency a 16.8.0. 💥
  - Verificare che la versione utilizzata nel progetto sia compatibile con la versione richiesta dal toolkit.
- Nuovo tipi TS: il kit è stato interamente riscritto in Typescript. 💥
- Componente `Pager`:
  - l'attributo `size` è stato rimosso poichè non utilizzato nel kit Bootstrap Italia. 💥
  - _Azioni richieste per vecchio comportamento_: utilizzare l'attributo `className` del componente `Pager`.
  - Rimosso componente `PagerList`: il componente `PagerList` è stato rimosso ed integrato all'interno del componente `Pager`. 💥
  - _Azioni richieste per vecchio comportamento_: rimuovere il componente `PagerList` ed utilizzare i nuovi attributi `listTag` e `listClassName` del componente `Pager`.
- Componente`Carousel`: rimosso componente e relativi sotto componenti 💥
  - _Qualora si volesse continuare ad usare il vecchio componente_, importare il componente `Carousel` da `reactstrap 8.9.x`.
- Componente `Hero`:
  - nuovo componente più fedele al design del kit Bootstrap Italia. 💥
  - È necessario sostituire il vecchio componente `Hero` con il nuovo componente. 💥
  - Nuovi componenti `HeroBackground`, `HeroBody`, `HeroButton`, `HeroCategory`, `HeroTitle`.
  - _Qualora si volesse continuare ad usare il vecchio componente_, importare il componente `Jumbotron` da `reactstrap 8.9.x`.
- Componente `Dimmer`:
  - il precedente attributo `className` non verrà più applicato anche al nodo wrapper. 💥
  - _Azioni richieste per vecchio comportamento_: impostare il nuovo attributo `wrapperClassName={true}` sul componente
- Componente `DimmerButton`:
  - ora l'attributo `dark` è `true` di default. 💥
  - _Azioni richieste per vecchio comportamento_: impostare `dark={false}` sul componente
- Componente `Icon`:
  - Le icone predefinite nel kit vengono ora caricate in modalità _lazy_. 💥
  - nuove funzioni di `preload` per le icone per situazioni di SSR e testing.
- Componente `Progress`:
  - il precedente attributo `className` non verrà più applicato anche al nodo wrapper. 💥
  - _Azioni richieste per vecchio comportamento_: impostare il nuovo attributo `wrapperClassName={true}` sul componente
- Componente `Section`:
  - il precedente attributo `className` non verrà più applicato anche al nodo wrapper. 💥
  - _Azioni richieste per vecchio comportamento_: impostare `wrapperClassName={true}` sul componente

### Nuovi componenti

- `Select`:
  - nuovo componente interno al kit.
- `Toolbar`:
  - nuovo componente, insieme a `ToolbarItem` e `ToolbarItemDivider`.
- Nuovo sistema di navscroll basato su hook `useNavScroll`.
  - Una nuova pagina di documentazione con esempi completi spiega come utilizzare il nuovo hook in combinazione con i componenti del kit.
- `TimeInput`:
  - nuovo componente.
- `Megamenu`:
  - nuovo componente.
- Nuovi componenti di layout introdotti per facilitare la costruzione di liste di immagini.
  - Nuovo componente `GridList`/`GridRow`/`GridItem`/`GridItemText`/`GridItemTextWrapper`.
  - Nuovo componente `ResponsiveImage` per immagini responsive.
- Nuovo sistema di notifiche aggiunto, con relativi test e documentazione.
  - Nuovo componente `NotificationManager`.
  - Nuova funzione `notify`.

### Nuove funzionalità aggiunte

- `Accordion`:
  - nuovo attributo `background` con valori `active` o `hover` aggiunto
  - nuovo attributo `iconLeft` da utilizzare per mostrare stato di apertura.
- `BottomNavItem`:
  - nuovi attributi `url` e `onLinkClick` per gestire l'azione di click con più dettaglio.
  - l'attributo `link` è stato deprecato 🗑
- `Breadcrumb`:
  - nuovo attributo `dark` per la visualizzazione del tema scuro.
- `Dimmer`:
  - nuovo attributo `wrapperClassName`.
- `Fontloader`:
  - nuovo attributo `fonts` per il caricamento di font aggiuntivi oltre a quelli standard.
- `Input`:
  - nuovo attributo `wrapperClassName` sostituisce la vecchia `wrapperClass` 🗑 (ora deprecato)
- `Icon`:
  - il componente supporta ora anche URL di immagini
- `Modal`:
  - nuovi attributi `withIcon`, `withLinkList` e `popConfirm` da utilizzato per diversi formati di modale.
- `ModalHeader`:
  - nuovo attributo `icon` da utilizzare per mostrare una icona nel titolo del modale.
- `Pager`:
  - nuovo attributo `total`.
  - nuovi attributi `listTag` e `listClassName` per il componente `Pager`.
- `Progress`:
  - nuovo attributo `wrapperClassName`.
- `Section`:
  - nuovo attributo `color`.
    - Gli attributi `neutral` e `muted` sono stati deprecati 🗑
- `Spinner`:
  - nuovo attributo `label` da utilizzare per mostrare stato di caricamento ai dispositivi screen reader.
- `SkipLinkItem`: nuovo attributo `focusable` per disabilitare il rendering on focus in casi specifici
- `StepperHeaderElement`:
  - nuovo valore `mobile` per l'attributo `variant`.
  - il precedente valore `steppers-index` è stato deprecato 🗑
  - nuovo attributo `appendIcon` che sostituisce la vecchia `icon` 🗑
  - nuovo attributo `prependIcon` che sostituisce la vecchia `iconName` 🗑
- `TimelinePin`:
  - nuovo attributo `iconName` che sostituisce la vecchia `icon` 🗑

### Bug fixes

- `BottomNavItem`: il tipo dell'attributo `badge` è stato corretto in `number`.
- `Input`: fix per la versione normalizzata del campo.
- `Section`: fix per classe di background quando nessun URL viene passato.

### Documentazione

Riscritta interamente la documentazione del kit per essere più completa e per offrire una consultazione più facile.

- Nuovo gruppo `Organizzare gli spazi` per una guida più completa sul tipo di layout a griglie offerto dal kit.
- Nuovo gruppo `Organizzare i contenuti` per una guida complete su `Liste`, `Tabelle` e `Tipografia`.
- I componenti specifici per la navigazione sono stati raggruppati nel gruppo `Menu di navigazione`.
- I componenti normalmente utilizzati per ricevere input dall'utente sono stati raggruppati nel gruppo `Form`.
- Tutti gli altri componenti sono stati inseriti nel gruppo `Componenti`.
- Nel gruppo `Utilities` sono presenti cataloghi completi, con relative guide, di colori ed icone presenti nel kit.

Infine due nuovi sezioni `Changelog` (pagina corrente) e `Domande Frequenti` sono state inserite per una informazione più completa del kit anche per aspetti non strettamente di sviluppo.

Per le pagine complete del kit dei comuni è stata dedicata una intera sezione `Templates` che verrà pian piano ampliata in futuro.
Tutte le storie presenti nella documentazione sono presenti singolarmente nella sezione `Componenti` in fondo, dove è possibile interagire con controlli dinamici per esempi più interattivi.

### Usi il Design React Kit in produzione? Scrivici!

Siamo felici di ricevere ogni tipo di feedback e commento se utilizzi il kit nella tu pagina/app/prodotto.
Facci sapere tramite il canale Developers Italia o tramite una issue cosa ne pensi.
